{
  "name": "isoman",
  "version": "0.3.7",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0"
  },
  "lint-staged": {
    "backend/**/*.go": [
      "make -C backend fmt-check",
      "make -C backend lint"
    ],
    "ui/**/*.{ts,tsx,js,jsx,json,css}": "cd ui && bun run check-only"
  }
}
